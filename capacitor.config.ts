import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'E8 Matematyka',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
